import { EmailModelDomainModel } from './email-model-domain-model';

describe('EmailModelDomainModel', () => {
  it('should create an instance', () => {
    expect(new EmailModelDomainModel()).toBeTruthy();
  });
});
