import { ContactdepartmentDirective } from './contactdepartment.directive';

describe('ContactdepartmentDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactdepartmentDirective();
    expect(directive).toBeTruthy();
  });
});
